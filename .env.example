# Add all of the environmental variables here instead of 
# embedding them directly in the app and utilize them 
# with the `DotEnv` package.

PORT=3000

# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL_FOR_MANAGEMENT="postgresql://{username}:{password}@localhost:5432/{dbname}?schema=management"
DATABASE_URL_COMMON="postgresql://{username}:{password}@localhost:5432/{dbname}?schema=common"
DATABASE_URL_WITHOUT_SCHEMA="postgresql://{username}:{password}@localhost:5432/{dbname}"